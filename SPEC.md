# SPEC — 도덕적 고려의 범위 메타버스 토론앱

중학교 2학년 도덕 수행평가(「도덕적 고려의 범위」, 40점) 직전, 학생들이 사상가 4명의 입장을
몸으로 익히게 하는 ZEP 스타일 웹앱. 한 반 28명 동시 접속, 태블릿 사용.

**이 문서는 장(章) 단위로 읽어라.** 한 번에 다 읽지 말고 지금 만들 장만 읽는다.

---

## 0. 기술 스택 (확정)

| 항목 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Next.js (App Router) + TypeScript | Vercel 배포가 한 번에 됨 |
| 실시간·로그인·DB | Supabase | 구글 로그인 + Realtime + Postgres가 한 곳에, 무료 |
| 렌더링 | HTML Canvas 2D (맵·캐릭터), 나머지는 DOM | 28명 움직여도 안 끊김 |
| 스타일 | CSS Modules 또는 plain CSS | Tailwind 안 씀 |
| 배포 | GitHub → Vercel | |

환경변수 2개만 쓴다: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`.

**그대로 가져다 쓸 파일 (새로 만들지 말 것):**
- `assets/sprites.js` — **28개 캐릭터 그림**(배경 제거 완료) + 캐릭터별 정보(CHAR_META) + 펫 4종 그림.
  **절대 열어보지 마라. 18만 자다. import만 해라.**
  `CHARS["ari_s3"]`가 그림, `CHAR_META["ari_s3"]`가 `{w,h,legTop,mode,name}`이다.
- `assets/thinkers.js` — 사상 데이터, 28캐릭터 카탈로그, 모둠 자리·달리기 규격, 갯벌 사례, 정렬 함수
- `assets/scoring.js` — 말풍선 자동 채점
- `prototype.html` — 네 탭이 다 도는 단일 파일 시제품. **그리기·움직임 코드는 여기서 그대로 옮겨 쓴다.**
- `supabase/schema.sql` — DB 스키마 (Supabase SQL Editor에 붙여넣기만)

---

## 1. 화면 구조

상단 탭 4개 + **모든 탭 왼쪽 위 고정 QR 코드**(현재 배포 URL). 태블릿에서 튕겨도 QR로 재접속.
QR은 `qrcode` npm 패키지로 클라이언트에서 그린다. 크기 88px, 접었다 펼 수 있게.

교사는 `?teacher=1` 쿼리 또는 로그인 이메일이 `rooms.teacher_uid`와 일치하면 교사 모드.
교사 모드에서만 보이는 것: 탭 강제 이동 버튼, 추첨 버튼, 채점 버튼, 모둠 구성 버튼, 말풍선 수정.

---

## 2. 1번 탭 — 입장 확인 (캐릭터 고르기)

1. 구글 로그인 필수. 로그인 안 하면 로그인 버튼만 보인다 (Supabase `signInWithOAuth({provider:'google'})`).
2. 로그인 즉시 28칸 그리드(가로 7 × 세로 4)를 띄운다. `CHARACTERS` 배열 순서 그대로.
   - 각 칸: 사상가 스프라이트 + 스타일 악세서리 + 사상가 이름표 + 고리 미리보기.
   - 이미 선택된 칸은 회색 처리 + 선택한 학생 이름 표시. **선착순**: `players` 테이블
     `unique(room_id, char_id)` 제약으로 보장. insert 실패하면 "이미 누가 골랐어요" 토스트 후 새로고침.
3. 선택 후 아래쪽에 **학생 정렬판**을 보여 준다.
   - `sortStudents()`로 학번 **내림차순**, 가로 **10명씩** 줄바꿈 (`GRID_PER_ROW`).
   - 학번은 구글 계정 이메일의 숫자를 뽑아 쓴다 (`studentNumber()`).
4. 각 학생 카드에 사상가 이름 + 고려 범위 한 줄(`thinker.scope`)을 같이 띄운다. 여기가 "입장 확인"이다.

### 캐릭터 그리는 법 (prototype.html의 drawChar를 그대로 옮겨라)
- 캐릭터 28개는 **이미 완성된 그림**이다. 소품(안경·모자·카메라·자전거 등)이 그림에 들어 있으니
  아이템을 따로 그리지 마라. 스타일 이름은 `CHAR_META[charId].name`.
- 달리기는 그림을 `legTop`에서 잘라 만든다. 위쪽(몸통)은 그대로 두고, 아래쪽을 좌우 반으로 나눠
  각각 엉덩이를 축으로 `±sin(phase)*RUN.swingRad`만큼 회전시킨 뒤 몸통을 덮어 그린다.
  자른 경계가 벌어지지 않도록 다리 조각은 `RUN.overlapPx`만큼 위에서부터 잘라 온다.
- `CHAR_META[charId].mode === "static"`인 세 캐릭터(스케이트보드·명상·자전거)는 다리를 자르지 말고
  통째로 그린 뒤 위아래로만 흔든다.
- 펫은 `PET_IMG` 그림을 쓴다. 진행 방향 뒤쪽으로 한 줄, 각자 다른 위상으로 통통 튄다.
  **아리스토텔레스 1마리 → 싱어 2 → 슈바이처 3 → 레오폴드 4.**

## 3. 2번 탭 — 벤다이어그램 만들기 (메타버스)

### 원을 이고 다닌다
**운동장에 벤다이어그램을 미리 그려 두지 않는다.** 각 캐릭터가 자기 크기의 원을 머리에 이고 다닌다.
아리스토텔레스가 가장 작고 레오폴드가 가장 크다(`thinker.ringR`). 걸을 때 원이 살짝 흔들린다.
이 원이 곧 그 사람의 도덕적 고려 범위다.

### 맵과 달리기
- 운동장 1900×1150. 잔디·모래만 있고 표식은 없다. 카메라는 내 캐릭터를 따라간다.
- **방향키나 화면 아래 십자키를 누르고 있는 동안 계속 달린다**(`RUN.speed`, px/초).
  한 칸씩 끊어 가는 방식이 아니다. 다리 흔들기 + 위아래 바운스 + 몸 기울기로 달리는 느낌을 낸다.
- 위치는 `players.x/y`에 200ms throttle로 쓰고 Realtime으로 전파. 다른 캐릭터는 lerp로 보간.

### 모이면 완성된다
- 자리(`MEET.spotCount` = 7곳)는 보이지 않는다. 다만 **내 모둠 자리에만** 반짝이는 작은 점과
  "우리 모둠 자리" 글씨를 띄워 어디로 갈지 알려 준다.
- 자리 반경 `MEET.joinRadius` 안에 들어오면 머리 위의 원이 **바닥으로 내려앉아** 자리 중심에 겹친다.
  큰 원부터 먼저 그려서 작은 원이 그 안에 들어가 보이게 한다.
- 서로 다른 사상가 네 명이 모두 들어오면 완성: 네 명 고정, 결계(청록 점선 링) 생성,
  다른 캐릭터는 그 안에 못 들어간다. 화면에 "인간 ⊂ 동물 ⊂ 생명 ⊂ 대지" 안내.

### 학생이 28명이 아닐 때
빈 캐릭터는 NPC가 되어 자기 자리로 천천히 달려간다. **내 자리의 내 몫은 비워 둔다.**

## 4. 3번 탭 — 토론

### 화면
- 위쪽: `CASE_AIRPORT`의 `title`, `q`, `situation`. 사례 카드는 항상 보인다.
- 가운데: **추첨판** — 접속한 학생 전원을 학번 내림차순으로 캐릭터 그림과 함께 깔아 둔다.
- 아래: 말풍선, 투표 버튼, 채점 결과.

### 추첨 연출 (교사용 버튼)
1. 시작 칸을 무작위로 정하고, 최소 한 바퀴를 돈 뒤 당첨자 칸에서 정확히 멈추도록
   **hop 횟수를 미리 계산한다**(`N = 칸수 + (당첨칸 - 시작칸)`). 그래야 오래 끌지 않고 확실히 멈춘다.
2. 칸을 옮길 때마다 노란 강조(`.hot`, 살짝 확대)와 **짧은 전자음**을 낸다. 음높이는 진행에 따라 올라간다.
3. 간격은 45ms에서 시작해 `45 + 280 × (진행률)³` ms로 점점 느려진다. 전체 3~5초.
4. 멈추면 그 칸이 초록으로 바뀌며 튀어오르고(`.win`), 4음 팡파르가 울린다.
5. 당첨자의 학번·사상가·고려 범위를 큰 글씨로 띄우고 입력창을 연다.
6. 소리 끄기 버튼을 둔다. 브라우저 정책상 AudioContext는 **버튼 클릭 시점에** 만든다.

### 그다음
- 뽑힌 학생만 입력창이 열린다(찬성/반대 + 근거). `claims`에 insert, 수정은 본인과 교사만.
- 제출하면 전체 화면 말풍선에 큰 글씨(최소 28px).
- 학생 투표(정확하다 / 아니다) → 실시간 막대.
- 교사가 채점 → `gradeClaim()` → **한 줄에 한 항목씩 세로로** 표시.

```
입장 정확        3 / 3
핵심 개념어      2 / 4
사상 혼용 없음   3 / 3
합계             8 / 10
```

## 5. 4번 탭 — 역할 바꿔 토론

1. 교사가 **모둠 짜기** 버튼 → 사상가가 서로 다른 4명씩 랜덤 팀 구성, `players.team_id`에 저장.
   같은 사상가끼리 묶이지 않게 사상가별 풀에서 하나씩 뽑는다. 남는 학생은 마지막 모둠에 5번째로.
2. 모둠 화면: 내 모둠 4명이 원형으로 배치. 가운데에 사례 카드.
3. **역할 교대**: "다음 차례" 버튼을 누를 때마다 모둠 안에서 캐릭터가 시계방향으로 한 칸씩 넘어간다.
   내 캐릭터가 친구의 사상가로 바뀌고, 화면 위에 **"지금 나는 ○○○(사상가)입니다"** 배너와
   그 사상의 `core` 한 문장, `keywords` 3개가 힌트로 뜬다.
4. 4번 돌면 모두가 네 사상가를 한 번씩 해 본 것. 마지막에 "네 사람 모두 경험 완료" 표시.

---

## 6. 만들 순서 (이대로)

1. Next.js 프로젝트 생성 + Supabase 클라이언트 + 구글 로그인 + 탭 껍데기 + QR
2. 1번 탭 (캐릭터 선착순 + 정렬판)
3. 2번 탭 (맵·이동·동심원 판·결계·NPC)
4. 3번 탭 (추첨·말풍선·투표·채점)
5. 4번 탭 (모둠·역할 교대)
6. GitHub push → Vercel 배포 → 구글 OAuth 리디렉션 URL 등록

각 단계가 끝나면 `npm run build`가 통과하는지 확인하고 커밋한다.

---

## 7. 지키는 규칙

- 사상가 표현은 `thinkers.js`의 문장만 쓴다. 새로 지어내지 않는다. 교과서 근거가 틀리면 수업이 망가진다.
- 학생 화면에 이메일 전체를 띄우지 않는다. 학번 숫자와 이름만.
- 28명이 동시에 움직여도 60fps를 유지한다. 캔버스는 1개, 리렌더는 requestAnimationFrame 1곳에서만.
- 접속이 끊기면 `players` 행은 남겨 둔다(캐릭터 유지). 재로그인하면 원래 캐릭터로 복귀.
